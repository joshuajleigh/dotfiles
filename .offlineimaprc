[general]
accounts = LFO

[Account LFO]
localrepository = Work-Local
remoterepository = Work-Remote

[Repository Work-Local]
type = Maildir
localfolders = ~/Maildir/LFO

[Repository Work-Remote]
type = IMAP
SSL = no
remotehost = localhost
remoteport = 1143
#cert_fingerprint =
maxconnections = 3
remoteuser = jleigh@leapfrogonline.com
remotepass = 481Nc0nv3n13n71
holdconnectionopen = true
keepalive = 120
realdelete = yes

# Folders to skip during sync. (You can set up which ones your self, this is just an example)
#folderfilter = lambda folder: folder in ['INBOX', 'Sent']
